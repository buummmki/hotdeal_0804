# 온누리 할인정보 — 워드프레스 사이드바 위젯

`.html` 파일을 브라우저로 열면 글자가 깨져 보입니다. charset 선언이 없는 조각 파일이라
브라우저가 인코딩을 잘못 추측해서 그렇고, 코드 자체는 정상입니다.
**아래 코드 블록에서 복사하시면 깨지지 않습니다.**

---

## 설치 방법

1. 워드프레스 관리자 → **외모 → 위젯**
2. **Main Sidebar** 열기
3. **사용자 정의 HTML** 위젯 추가
4. 아래 코드를 통째로 붙여넣고 저장

한 번 넣으면 이후 관리할 게 없습니다. 사이트가 딜을 수집하면 위젯도 자동으로 바뀝니다.

---

## 붙여넣을 코드

```html
<div id="onnuri-sale-widget" data-cat="food" data-limit="5" style="border:1px solid #e5e7eb;border-radius:12px;overflow:hidden;background:#fff;font-family:-apple-system,BlinkMacSystemFont,'Malgun Gothic','맑은 고딕',sans-serif;">
  <div style="background:#111827;color:#fff;padding:12px 14px;">
    <div style="font-size:15px;font-weight:800;letter-spacing:-0.02em;">온누리 <span style="color:#f43f5e;">할인정보</span></div>
    <div style="font-size:11px;color:#9ca3af;margin-top:2px;">오늘의 식품 특가 · 매일 갱신</div>
  </div>
  <div id="onnuri-sale-list" style="padding:6px 0;">
    <div style="padding:16px 14px;font-size:12px;color:#9ca3af;">불러오는 중…</div>
  </div>
  <a href="https://sale.onnuriinfo.com/food" target="_blank" rel="noopener" style="display:block;padding:11px 14px;background:#f9fafb;border-top:1px solid #e5e7eb;text-align:center;font-size:13px;font-weight:700;color:#f43f5e;text-decoration:none;">특가 전체 보기 &rsaquo;</a>
</div>
<script>
(function () {
  var box = document.getElementById('onnuri-sale-widget');
  var list = document.getElementById('onnuri-sale-list');
  if (!box || !list) return;
  var cat = box.getAttribute('data-cat') || 'food';
  var limit = box.getAttribute('data-limit') || 5;
  function esc(s) {
    return String(s).replace(/[&<>"]/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c];
    });
  }
  fetch('https://sale.onnuriinfo.com/api/widget?cat=' + cat + '&limit=' + limit)
    .then(function (r) { return r.json(); })
    .then(function (data) {
      if (!data.deals || !data.deals.length) throw new Error('empty');
      list.innerHTML = data.deals.map(function (d, i) {
        var price = d.price ? esc(d.price) : '가격 미표기';
        var ship = d.freeShipping ? '<span style="color:#059669;font-size:10px;margin-left:5px;">무료배송</span>' : '';
        var border = i === 0 ? '' : 'border-top:1px solid #f3f4f6;';
        return '<a href="' + esc(d.url) + '" target="_blank" rel="noopener" style="display:flex;gap:8px;padding:9px 14px;text-decoration:none;color:inherit;' + border + '">' +
          '<span style="flex:0 0 14px;font-size:11px;font-weight:800;color:' + (i < 3 ? '#f43f5e' : '#d1d5db') + ';line-height:1.5;">' + (i + 1) + '</span>' +
          '<span style="flex:1;min-width:0;">' +
            '<span style="display:-webkit-box;-webkit-line-clamp:2;-webkit-box-orient:vertical;overflow:hidden;font-size:12.5px;line-height:1.45;color:#111827;">' + esc(d.title) + '</span>' +
            '<span style="display:block;margin-top:3px;font-size:11px;color:#6b7280;"><b style="color:#f43f5e;">' + price + '</b>' + ship + '</span>' +
          '</span></a>';
      }).join('');
    })
    .catch(function () {
      list.innerHTML = '<div style="padding:14px;font-size:12px;color:#6b7280;line-height:1.6;">커뮤니티에 올라온 식품·먹거리 특가를 모아 매일 정리합니다.<br>장 보기 전에 한 번 확인해 보세요.</div>';
    });
})();
</script>
```

---

## 설정 바꾸기

첫 줄의 두 값만 고치면 됩니다.

**`data-cat`** — 어떤 카테고리를 보여줄지

| 값 | 내용 |
|---|---|
| `food` | 식품 (기본값 — 먹거리 콘텐츠와 가장 잘 맞습니다) |
| `life` | 생활·패션 |
| `appliance` | 가전 |
| `coupon` | 쿠폰·상품권 |
| `all` | 전체 |

**`data-limit`** — 보여줄 개수 (1~10, 기본 5)

사이드바가 길어 여유가 있으면 7~8개로 늘려도 됩니다. 다만 너무 길면 아래 다른 위젯이
화면 밖으로 밀리니 5~7 사이를 권합니다.

---

## 확인 사항

- **스크립트가 막혀도 안전합니다.** API가 응답하지 않으면 안내 문구와 "특가 전체 보기"
  링크로 대체됩니다. 빈 상자가 남지 않습니다.
- 일부 워드프레스 보안 플러그인이 위젯 안의 `<script>`를 제거합니다. 저장 후 화면에
  목록이 안 뜨면 그 경우이니 알려주세요. 스크립트 없는 정적 버전으로 바꿔 드리겠습니다.
- 캐시 플러그인을 쓰신다면 저장 후 캐시를 한 번 비워야 바로 보입니다.
