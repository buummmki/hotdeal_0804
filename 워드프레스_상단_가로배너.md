# 온누리 할인정보 — 상단 가로 배너 (모바일 대응)

**Header Banner Ad Section** 에 넣는 한 줄짜리 배너입니다.
PC에서는 한 줄로 쭉 펼쳐지고, 모바일에서는 손가락으로 좌우로 넘겨볼 수 있습니다.

사이드바 위젯(세로 목록)과 **같이 써도 됩니다.** 서로 다른 자리라 중복으로 보이지 않습니다.

---

## 설치 방법

1. 워드프레스 관리자 → **외모 → 위젯**
2. **Header Banner Ad Section** 열기
3. **사용자 정의 HTML** 위젯 추가
4. 아래 코드를 통째로 붙여넣고 저장

---

## 붙여넣을 코드

```html
<div id="onnuri-strip" data-cat="food" data-limit="6" style="margin:0 auto;max-width:1200px;padding:8px 12px;font-family:-apple-system,BlinkMacSystemFont,'Malgun Gothic','맑은 고딕',sans-serif;">
  <div style="display:flex;align-items:center;gap:10px;border:1px solid #e5e7eb;border-radius:10px;background:#fff;padding:8px 10px;">
    <a href="https://sale.onnuriinfo.com/food" target="_blank" rel="noopener" style="flex:0 0 auto;display:flex;align-items:center;gap:6px;text-decoration:none;">
      <span style="display:inline-block;background:#f43f5e;color:#fff;font-size:10px;font-weight:800;padding:3px 7px;border-radius:5px;white-space:nowrap;">오늘의 특가</span>
    </a>
    <div id="onnuri-strip-list" style="flex:1;min-width:0;display:flex;gap:14px;overflow-x:auto;scrollbar-width:none;-ms-overflow-style:none;">
      <span style="font-size:12px;color:#9ca3af;white-space:nowrap;">불러오는 중…</span>
    </div>
    <a href="https://sale.onnuriinfo.com/food" target="_blank" rel="noopener" style="flex:0 0 auto;font-size:12px;font-weight:700;color:#f43f5e;text-decoration:none;white-space:nowrap;">전체보기 &rsaquo;</a>
  </div>
</div>
<style>#onnuri-strip-list::-webkit-scrollbar{display:none}</style>
<script>
(function () {
  var box = document.getElementById('onnuri-strip');
  var list = document.getElementById('onnuri-strip-list');
  if (!box || !list) return;
  var cat = box.getAttribute('data-cat') || 'food';
  var limit = box.getAttribute('data-limit') || 6;
  function esc(s) {
    return String(s).replace(/[&<>"]/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c];
    });
  }
  fetch('https://sale.onnuriinfo.com/api/widget?cat=' + cat + '&limit=' + limit)
    .then(function (r) { return r.json(); })
    .then(function (data) {
      if (!data.deals || !data.deals.length) throw new Error('empty');
      list.innerHTML = data.deals.map(function (d) {
        var t = d.title.length > 26 ? d.title.slice(0, 26) + '…' : d.title;
        var price = d.price ? '<b style="color:#f43f5e;margin-left:5px;">' + esc(d.price) + '</b>' : '';
        return '<a href="' + esc(d.url) + '" target="_blank" rel="noopener" style="flex:0 0 auto;font-size:12.5px;color:#374151;text-decoration:none;white-space:nowrap;">' + esc(t) + price + '</a>';
      }).join('');
    })
    .catch(function () {
      list.innerHTML = '<a href="https://sale.onnuriinfo.com/food" target="_blank" rel="noopener" style="font-size:12.5px;color:#374151;text-decoration:none;white-space:nowrap;">커뮤니티 식품·먹거리 특가 모음 — 매일 갱신</a>';
    });
})();
</script>
```

---

## 자리별 특성 정리

| 위젯 영역 | PC | 모바일 | 추천도 |
|---|---|---|---|
| **Header Banner Ad Section** | 상단 가로 | **상단 그대로 노출** | 모바일 노출이 필요하면 여기 |
| **Main Sidebar** | 우측 세로 | 본문 아래로 내려감 | PC 전환율이 가장 좋음 |
| Off Canvas Drawer Menu | 햄버거 메뉴 안 | 햄버거 메뉴 안 | 메뉴를 연 사람만 봄 |
| Footer Widget 1~3 | 최하단 | 최하단 | 거의 안 보임 |

**Header Banner + Main Sidebar 조합**을 권합니다. 상단 가로 배너로 모바일 노출을 잡고,
PC에서는 사이드바 세로 목록이 맥락 있는 클릭을 만듭니다.

---

## 주의할 점

- 상단 배너는 모든 페이지에 나오므로 **너무 크면 첫인상이 광고 사이트처럼** 됩니다.
  그래서 높이를 40px 정도로 낮게 잡았습니다. 임의로 키우지 않는 게 좋습니다.
- 보여줄 개수는 `data-limit` 으로 조절합니다. 모바일에서는 어차피 옆으로 넘겨야 하니
  6개 정도가 적당합니다.
- 테마에 따라 헤더 영역 배경이 어두울 수 있습니다. 배너가 배경에 묻히면 알려주세요.
  흰 상자 대신 어두운 배경 버전으로 바꿔 드리겠습니다.
